services:
  portfolio:
    build: .
    container_name: portfolio
    restart: unless-stopped
    environment:
      NODE_ENV: "production"
      PORT: "4000"
      HOST: "0.0.0.0"
    ports:
      - "127.0.0.1:4000:4000"
